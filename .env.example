# Copy to .env and fill in. .env is gitignored.
# Or: render from 1Password via `projects-secrets pull`.

FSQ_KEY=
SERVICE_DOMAIN=
# Optional. Foursquare Iceberg catalog endpoint.
# Defaults to https://catalog.h3-hub.foursquare.com/iceberg when unset.
FSQ_ICEBERG_ENDPOINT=
